/*
 * Site Messages
 *
 * This contains all the text for the Site component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.Site';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'React Material UI Starter',
  },
});
